package org.atlasalign.application.dg;

/** Frozen D01 ablations over an identical candidate table. */
public enum DgRankingMethod {
    WHOLE_SECTION_BASELINE,
    DG_ONLY,
    DG_AUGMENTED,
    MULTI_FEATURE
}
