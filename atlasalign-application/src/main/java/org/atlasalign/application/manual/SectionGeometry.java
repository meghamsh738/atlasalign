package org.atlasalign.application.manual;

/** Image-side geometry only; it does not imply anatomical laterality. */
public enum SectionGeometry {
    FULL,
    IMAGE_LEFT_HALF,
    IMAGE_RIGHT_HALF,
    PARTIAL_OR_DAMAGED
}
