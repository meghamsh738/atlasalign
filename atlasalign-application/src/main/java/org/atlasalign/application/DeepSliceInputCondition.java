package org.atlasalign.application;

/** Exact application-side preparation supplied to the DeepSlice provider. */
public enum DeepSliceInputCondition {
    ORIGINAL_PREVIEW,
    VIRTUAL_HALF_FULL_CANVAS,
    VIRTUAL_HALF_TIGHT_CROP
}
