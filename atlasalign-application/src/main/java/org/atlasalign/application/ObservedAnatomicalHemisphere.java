package org.atlasalign.application;

/**
 * Anatomical hemisphere derived only after orientation confirmation.
 */
public enum ObservedAnatomicalHemisphere {
    BOTH,
    LEFT,
    RIGHT,
    UNSURE
}
